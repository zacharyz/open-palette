$('.main').html("<%= escape_javascript(render 'shared/feed_posts') %>");

$(function(){        
  var $container = $('.posts');
  $container.imagesLoaded(function(){
    $container.masonry({
      itemSelector : '.item',
      columnWidth : 240
    });
  });  
  
});
