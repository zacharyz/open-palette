%li
  = link_to image_tag(post.image_url(:thumb).to_s, :alt=> post.title,:class => "thumbnail", :alt=> post.title),post_path(post),:class=>"post",:title=> feed_item.title, :"data-content" => "by <strong>#{feed_item.user.name}</strong> - #{pluralize(feed_item.hearts.count, 'like')}
               