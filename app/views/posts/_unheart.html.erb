<%= form_for current_user.relationships.find_by_hearted_id(@post),
             :html => { :method => :delete },
             :remote => true do |f| %>
  <div class="actions"><%= f.submit "Unheart" %></div>
<% end %>
