<% @user ||= current_user %>
<div class="user_info group">
  <div id ="user_image">     
     <a href="<%= user_path(@user) %>"><%= gravatar_for @user, :size => 40 %></a>     
  </div>
  <span class="user_name"><%= @user.name %></span>                                                                     
  <%= render 'shared/stats' %> 
</div>   

