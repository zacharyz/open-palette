%h5 Popular Tags                                           
.row
  %ol.tags
    - for tag in @tags
      %li.tag
        = link_to tag.name, { :action=> :tag, :id => tag.name, :controller => :posts }, :remote => true