- unless @posts.empty?
  - if @title
    %h2 
      = @title
  %ul.media-grid.posts
    - for post in @posts
      = render :partial => "shared/feed_item", :locals => { :feed_item => post}
    
- else
  %p No Posts!